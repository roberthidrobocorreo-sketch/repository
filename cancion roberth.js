const prompt=require("prompt-sync")();
const dibujar=require("./modulo.js");

const a=("Oye Andrea");
const b=("Perra Malparida");
const c=("Ya me dijeron que estas");
const d=("EN EL JPTA CARTAGENA");
const e=("CAREVRGA");
const f=("Claro, como no vas a estar en Cartagena MALPARIDA");
const g=("SI TE ROBASTE EL JPTA VESTIDO DE BAÑO MIO");
const h=("MALPARIDA JPTA");
const j=("Que pensaste?");
const k=("Que nunca me iba a dar cuenta que me habias robado el MALPARIDO VESTIDO DE BAÑO?");
const l=("Claro!!!");
const m=("De la unica forma que puedes ir a CARTAGENA, SANTA MARTA, TOA´ VRGA");
const n=("ES ROBANDOTE LAS MONDA...");

console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<a.length;i++)
{
    let caracter= a[i];
    dibujar(i,2,caracter);
    sleep(75);
}

console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<b.length;i++)
{
    let caracter= b[i];
    dibujar(i,2,caracter);
    sleep(75);
}
console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<c.length;i++)
{
    let caracter= c[i];
    dibujar(i,2,caracter);
    sleep(40);
}
console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<d.length;i++)
{
    let caracter= d[i];
    dibujar(i,2,caracter);
    sleep(50);
}
console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<e.length;i++)
{
    let caracter= e[i];
    dibujar(i,2,caracter);
    sleep(100);
}
console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<f.length;i++)
{
    let caracter= f[i];
    dibujar(i,2,caracter);
    sleep(45);
}
console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<g.length;i++)
{
    let caracter= g[i];
    dibujar(i,2,caracter);
    sleep(50);
}
console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<h.length;i++)
{
    let caracter= h[i];
    dibujar(i,2,caracter);
    sleep(80);
}
console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<j.length;i++)
{
    let caracter= j[i];
    dibujar(i,2,caracter);
    sleep(50);
}
console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<k.length;i++)
{
    let caracter= k[i];
    dibujar(i,2,caracter);
    sleep(30);
}
console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<e.length;i++)
{
    let caracter= e[i];
    dibujar(i,2,caracter);
    sleep(85);
}
console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<m.length;i++)
{
    let caracter= m[i];
    dibujar(i,2,caracter);
    sleep(35);
}
console.clear();

function sleep (ms){
    Atomics.wait (new Int32Array(new SharedArrayBuffer(4)),0,0,ms);
}

for (let i=0;i<n.length;i++)
{
    let caracter= n[i];
    dibujar(i,2,caracter);
    sleep(45);
}

console.log("\n\nDedicado para las Andreas de todo el Perú");

